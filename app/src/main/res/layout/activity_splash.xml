<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/rl_splash"
    android:background="@color/md_classic_background">

    <androidx.cardview.widget.CardView
        android:id="@+id/cv_splash_card"
        app:cardElevation="@dimen/_5sdp"
        app:cardCornerRadius="@dimen/_10sdp"
        android:layout_width="@dimen/_130sdp"
        android:layout_height="@dimen/_130sdp"
        android:layout_centerInParent="true">

        <ImageView
            android:id="@+id/iv_splash_logo"
            android:src="@drawable/logo"
            android:scaleType="centerCrop"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/default_todo" />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tv_splash_title"
        android:layout_below="@+id/cv_splash_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/_15ssp"
        android:textColor="@color/md_classic_title"
        android:layout_marginTop="@dimen/_10sdp"
        android:text="@string/app_name"
        android:layout_centerHorizontal="true"/>

    <ProgressBar
        android:id="@+id/pb_splash"
        android:layout_width="@dimen/_40sdp"
        android:layout_height="@dimen/_40sdp"
        android:indeterminate="true"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/_20sdp" />

</RelativeLayout>